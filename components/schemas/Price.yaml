title: Price
type: object
properties:
  starting_quantity:
    type:
      - integer
      - string
  ending_quantity:
    type:
      - integer
      - string
      - "null"
  unit_price:
    type:
      - number
      - string
    description: The price can contain up to 8 decimal places. i.e. 1.00 or 0.0012 or 0.00000065
required:
  - starting_quantity
  - unit_price

title: Invoice Event Base
type: object
properties:
  id:
    type: integer
    format: int64
  timestamp:
    type: string
    format: date-time
  invoice:
    $ref: "../Invoice.yaml"
required:
  - id
  - timestamp
  - invoice

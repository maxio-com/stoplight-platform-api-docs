title: Proforma Bad Request Error Response
type: object
properties:
  errors:
    $ref: "./Proforma-Error.yaml"
x-examples:
  example-1:
    errors:
      subscription:
        base:
          - must be an object

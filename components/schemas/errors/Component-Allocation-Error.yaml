allOf:
  - $ref: "./Component-Error.yaml"
  - type: object
    x-examples:
      example-1:
        errors:
          - kind: allocation
            component_id: 379512
            "on": base
            message: Allocations can only be updated for quantity and on/off components.
    properties:
      errors:
        type: array
        items:
          type: object
          properties:
            kind:
              type: string
            "on":
              type: string

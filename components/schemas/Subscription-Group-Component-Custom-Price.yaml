title: Subscription Group Component Custom Price
allOf:
  - $ref: "./Pricing.yaml"
    type: object
    description: Used in place of `price_point_id` to define a custom price point unique to the subscription. You still need to provide `component_id`.
    properties:
      overage_pricing:
        type: array
        items:
          $ref: "./Pricing.yaml"

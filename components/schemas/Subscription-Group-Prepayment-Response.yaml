title: Subscription Group Prepayment Response
type: object
properties:
  id:
    type: integer
  amount_in_cents:
    type: integer
    format: int64
    description: The amount in cents of the entry.
  ending_balance_in_cents:
    type: integer
    format: int64
    description: The ending balance in cents of the account.
  entry_type:
    $ref: "./Service-Credit-Type.yaml"
  memo:
    type: string
    description: |
      A memo attached to the entry.

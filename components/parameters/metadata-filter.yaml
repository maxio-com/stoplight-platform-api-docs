in: query
name: metadata[field_name]
schema:
  type: array
  items:
    type: string
    description: |-
      The value of the metadata field specified in the parameter.
  example:
    - value
style: form
explode: false
description: |-
  Use in query metadata[my-field]=value&metadata[other-field]=another_value.
